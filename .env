# Local folder (default)
MLFLOW_TRACKING_URI=mlruns

# HTTP endpoint (remote MLflow server)
MLFLOW_TRACKING_URI=http://127.0.0.1:5000

# Local PostgreSQL backend store
#POSGRES_URI=postgresql+psycopg2://mlflow:new_secure_password@localhost:5432/usr_mlflow
## actualmente en uso 
MLFLOW_TRACKING_URI=sqlite:///backend.db
